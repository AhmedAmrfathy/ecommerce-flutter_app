import 'package:flutter/material.dart';

class CategoryScreens extends StatelessWidget {
  static const categoryroute='categoryroute';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('categorypage'),
    );
  }
}
