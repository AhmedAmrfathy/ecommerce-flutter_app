import 'package:flutter/material.dart';

class AcountScreens extends StatelessWidget {
  static const acountroute='acountroute';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('AcountScreens'),
    );
  }
}
